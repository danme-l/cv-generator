import React, { Component } from "react";

class Instructions extends Component {
    render() {
        return (
            <div>
                Hover over a component to edit it
            </div>
        )
    }
}

export default Instructions;