import React, { Component } from "react";

class Util extends Component {
    render() {
        return (
            <div>
                Hover over a component to edit it
            </div>
        )
    }
}

export default Util;